[Mysql]
  用户名：root
  密码：123456
  
  导入数据：http://localhost/phpmyadmin/ 上方菜单使用导入功能


[Apache]
1. httpd.conf文件

a.这里如果是none 改为All

<Directory />
    AllowOverride All
    ...
</Directory>

b.如果这行前面有注释符号，将注释符号'#'去掉

LoadModule rewrite_module modules/mod_rewrite.so

[其他]
  框架自动加载的库在config/autoload.php
  base_url等在config/config.php
  数据库设置在config/database.php (尽量不要改)
  